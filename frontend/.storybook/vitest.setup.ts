import { setProjectAnnotations } from '@storybook/react';
import * as a11yAddonAnnotations from '@storybook/addon-a11y/preview';

// Storybook의 프로젝트 애노테이션 설정
setProjectAnnotations([a11yAddonAnnotations]);
